spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bookmyshow
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: localhost
    port: 6379
server:
  port: 8080